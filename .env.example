# ===========================================
# Vibe-Git Docker 部署配置
# ===========================================

# 项目路径（必填）- 映射到工作容器
PROJECT_PATH=/path/to/your/project

# Claude Gateway 配置
ANTHROPIC_API_KEY=sk-ant-api-your-anthropic-api-key
GATEWAY_TOKEN=vibe-git-secret-token

# Worker 配置
WORKER_TOKEN=worker-secret-token

# 可选：自定义端口
# GATEWAY_PORT=8080
# WORKER_HTTP_PORT=3000

# 可选：GitHub Token（用于 vibe-git 主程序）
# GITHUB_TOKEN=ghp_your_github_token
